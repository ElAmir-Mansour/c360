// IT DR · Live recovery (failover/failback + recovery workbench).
//
// New canonical location for the legacy `/dr/recover` surface; the legacy path
// permanently redirects here. Re-exports the existing page — no logic is forked.
export { default } from '../../../dr/recover/page';
