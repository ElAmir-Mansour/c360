# Clario 360 — WebSocket upgrade map (installed to /etc/nginx/conf.d/).
# Uses a uniquely-named variable so it can never clash with a $connection_upgrade
# map another vhost on this shared box may already define.
map $http_upgrade $clario_connection_upgrade {
    default upgrade;
    ''      close;
}
