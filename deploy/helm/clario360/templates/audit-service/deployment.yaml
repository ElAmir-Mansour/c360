{{- if .Values.auditService.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "clario360.componentName" (dict "root" . "component" "audit-service") }}
  namespace: {{ include "clario360.namespace" . }}
  labels:
    {{- include "clario360.componentLabels" (dict "root" . "component" "audit-service") | nindent 4 }}
spec:
  replicas: {{ .Values.auditService.replicaCount | default 2 }}
  revisionHistoryLimit: {{ .Values.auditService.revisionHistoryLimit | default 5 }}
  selector:
    matchLabels:
      {{- include "clario360.componentSelectorLabels" (dict "root" . "component" "audit-service") | nindent 6 }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/configmap-services.yaml") . | sha256sum }}
        {{- include "clario360.prometheusAnnotations" (dict "metricsPort" "9084") | nindent 8 }}
        {{- with .Values.auditService.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      labels:
        {{- include "clario360.componentSelectorLabels" (dict "root" . "component" "audit-service") | nindent 8 }}
        {{- with .Values.auditService.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "clario360.serviceAccountName" (dict "root" . "component" "audit-service" "svcConfig" .Values.auditService) }}
      securityContext:
        {{- include "clario360.podSecurityContext" . | nindent 8 }}
      terminationGracePeriodSeconds: {{ .Values.auditService.terminationGracePeriodSeconds | default 30 }}
      containers:
        - name: audit-service
          image: {{ include "clario360.image" (dict "imageConfig" .Values.auditService.image "root" . "defaultRepo" "clario360/audit-service") }}
          imagePullPolicy: {{ .Values.auditService.image.pullPolicy | default "IfNotPresent" }}
          securityContext:
            {{- include "clario360.containerSecurityContext" . | nindent 12 }}
          ports:
            - name: http
              containerPort: 8084
              protocol: TCP
            - name: admin
              containerPort: 9084
              protocol: TCP
          envFrom:
            - configMapRef:
                name: {{ include "clario360.sharedConfigMapName" . }}
            - configMapRef:
                name: {{ include "clario360.componentName" (dict "root" . "component" "audit-service") }}-config
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: {{ include "clario360.fullname" . }}-database-credentials
                  key: PLATFORM_DB_URL
            - name: REDIS_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "clario360.fullname" . }}-redis-credentials
                  key: REDIS_PASSWORD
            - name: AUTH_RSA_PUBLIC_KEY_PEM
              valueFrom:
                secretKeyRef:
                  name: {{ include "clario360.fullname" . }}-jwt-keys
                  key: AUTH_RSA_PUBLIC_KEY_PEM
            - name: AUDIT_MINIO_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ include "clario360.fullname" . }}-minio-credentials
                  key: MINIO_ACCESS_KEY
            - name: AUDIT_MINIO_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ include "clario360.fullname" . }}-minio-credentials
                  key: MINIO_SECRET_KEY
            - name: KAFKA_BROKERS
              valueFrom:
                configMapKeyRef:
                  name: {{ include "clario360.sharedConfigMapName" . }}
                  key: KAFKA_BROKERS
            - name: OTEL_SERVICE_NAME
              value: "audit-service"
            {{- with .Values.auditService.extraEnv }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          {{- include "clario360.healthProbes" (dict "portName" "http" "healthPath" "/healthz" "readyPath" "/readyz") | nindent 10 }}
          resources:
            {{- toYaml .Values.auditService.resources | nindent 12 }}
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir: {}
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: {{ if eq (default "dev" .Values.global.environment) "production" }}DoNotSchedule{{ else }}ScheduleAnyway{{ end }}
          labelSelector:
            matchLabels:
              {{- include "clario360.componentSelectorLabels" (dict "root" . "component" "audit-service") | nindent 14 }}
      {{- with .Values.auditService.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.auditService.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.auditService.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
