# Clario 360 SDK

Python SDK for the Clario 360 secure notebook workspace. The package is
installed from local source into the notebook images so the SDK stays aligned
with the platform release deployed alongside JupyterHub.
