// IT DR · Runbook studio + catalog.
//
// New canonical location for the legacy `/dr/runbooks` surface; the legacy path
// permanently redirects here. Re-exports the existing page — no logic is forked.
export { default } from '../../../dr/runbooks/page';
