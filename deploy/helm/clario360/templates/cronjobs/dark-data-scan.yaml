{{- if .Values.cronjobs.darkDataScan.enabled }}
apiVersion: batch/v1
kind: CronJob
metadata:
  name: {{ include "clario360.fullname" . }}-dark-data-scan
  namespace: {{ include "clario360.namespace" . }}
  labels:
    {{- include "clario360.labels" . | nindent 4 }}
    app.kubernetes.io/component: cronjob-dark-data-scan
spec:
  schedule: {{ .Values.cronjobs.darkDataScan.schedule | quote }}
  concurrencyPolicy: Forbid
  failedJobsHistoryLimit: 3
  successfulJobsHistoryLimit: 3
  startingDeadlineSeconds: 300
  jobTemplate:
    spec:
      activeDeadlineSeconds: {{ .Values.cronjobs.darkDataScan.activeDeadlineSeconds | default 600 }}
      backoffLimit: {{ .Values.cronjobs.darkDataScan.backoffLimit | default 2 }}
      template:
        metadata:
          labels:
            app.kubernetes.io/component: cronjob-dark-data-scan
            app.kubernetes.io/part-of: clario360
        spec:
          restartPolicy: OnFailure
          {{- with .Values.global.imagePullSecrets }}
          imagePullSecrets:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          securityContext:
            runAsNonRoot: true
            runAsUser: 65534
            runAsGroup: 65534
            fsGroup: 65534
            seccompProfile:
              type: RuntimeDefault
          containers:
            - name: dark-data-scan
              {{- if .Values.cronjobs.darkDataScan.image }}
              image: {{ include "clario360.image" (dict "imageConfig" .Values.cronjobs.darkDataScan.image "root" . "defaultRepo" .Values.cronjobs.darkDataScan.image.repository) }}
              {{- else }}
              image: {{ include "clario360.image" (dict "imageConfig" .Values.dataService.image "root" . "defaultRepo" .Values.dataService.image.repository) }}
              {{- end }}
              imagePullPolicy: {{ .Values.global.imagePullPolicy | default "IfNotPresent" }}
              command: ["/app"]
              args: ["--job=dark-data-scan"]
              env:
                - name: DATA_DB_URL
                  valueFrom:
                    secretKeyRef:
                      name: {{ include "clario360.fullname" . }}-database-credentials
                      key: DATA_DB_URL
                - name: DATA_REDIS_URL
                  valueFrom:
                    configMapKeyRef:
                      name: {{ include "clario360.sharedConfigMapName" . }}
                      key: REDIS_HOST
                - name: REDIS_PASSWORD
                  valueFrom:
                    secretKeyRef:
                      name: {{ .Values.redis.existingSecret }}
                      key: REDIS_PASSWORD
                - name: DATA_KAFKA_BROKERS
                  valueFrom:
                    configMapKeyRef:
                      name: {{ include "clario360.sharedConfigMapName" . }}
                      key: KAFKA_BROKERS
                - name: DATA_MINIO_ENDPOINT
                  valueFrom:
                    configMapKeyRef:
                      name: {{ include "clario360.sharedConfigMapName" . }}
                      key: MINIO_ENDPOINT
                - name: DATA_MINIO_USE_SSL
                  valueFrom:
                    configMapKeyRef:
                      name: {{ include "clario360.sharedConfigMapName" . }}
                      key: MINIO_USE_SSL
                - name: DATA_MINIO_ACCESS_KEY
                  valueFrom:
                    secretKeyRef:
                      name: {{ .Values.minio.existingSecret }}
                      key: MINIO_ACCESS_KEY
                - name: DATA_MINIO_SECRET_KEY
                  valueFrom:
                    secretKeyRef:
                      name: {{ .Values.minio.existingSecret }}
                      key: MINIO_SECRET_KEY
                - name: OTEL_SERVICE_NAME
                  value: "dark-data-scan"
              securityContext:
                allowPrivilegeEscalation: false
                readOnlyRootFilesystem: true
                capabilities:
                  drop:
                    - ALL
              resources:
                {{- toYaml .Values.cronjobs.darkDataScan.resources | nindent 16 }}
              volumeMounts:
                - name: tmp
                  mountPath: /tmp
          volumes:
            - name: tmp
              emptyDir: {}
{{- end }}
