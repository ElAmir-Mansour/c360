{{- if .Values.dataService.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "clario360.componentName" (dict "root" . "component" "data-service") }}
  namespace: {{ include "clario360.namespace" . }}
  labels:
    {{- include "clario360.componentLabels" (dict "root" . "component" "data-service") | nindent 4 }}
spec:
  {{- if not ((.Values.dataService).autoscaling).enabled }}
  replicas: {{ .Values.dataService.replicaCount | default 2 }}
  {{- end }}
  revisionHistoryLimit: {{ .Values.dataService.revisionHistoryLimit | default 5 }}
  selector:
    matchLabels:
      {{- include "clario360.componentSelectorLabels" (dict "root" . "component" "data-service") | nindent 6 }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/data-service/configmap.yaml") . | sha256sum }}
        {{- include "clario360.prometheusAnnotations" (dict "metricsPort" "9090") | nindent 8 }}
        {{- with .Values.dataService.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      labels:
        {{- include "clario360.componentSelectorLabels" (dict "root" . "component" "data-service") | nindent 8 }}
        {{- with .Values.dataService.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "clario360.serviceAccountName" (dict "root" . "component" "data-service" "svcConfig" .Values.dataService) }}
      securityContext:
        {{- include "clario360.podSecurityContext" . | nindent 8 }}
      terminationGracePeriodSeconds: {{ .Values.dataService.terminationGracePeriodSeconds | default 30 }}
      containers:
        - name: data-service
          image: {{ include "clario360.image" (dict "imageConfig" .Values.dataService.image "root" . "defaultRepo" "clario360/data-service") }}
          imagePullPolicy: {{ .Values.dataService.image.pullPolicy | default "IfNotPresent" }}
          securityContext:
            {{- include "clario360.containerSecurityContext" . | nindent 12 }}
          ports:
            - name: http
              containerPort: 8091
              protocol: TCP
            - name: metrics
              containerPort: 9090
              protocol: TCP
          envFrom:
            - configMapRef:
                name: {{ include "clario360.sharedConfigMapName" . }}
            - configMapRef:
                name: {{ include "clario360.componentName" (dict "root" . "component" "data-service") }}-config
          env:
            - name: DATA_DB_URL
              valueFrom:
                secretKeyRef:
                  name: {{ include "clario360.fullname" . }}-database-credentials
                  key: DATA_DB_URL
            - name: DATA_REDIS_URL
              valueFrom:
                secretKeyRef:
                  name: {{ include "clario360.fullname" . }}-redis-credentials
                  key: REDIS_URL
            - name: DATA_KAFKA_BROKERS
              valueFrom:
                configMapKeyRef:
                  name: {{ include "clario360.sharedConfigMapName" . }}
                  key: KAFKA_BROKERS
            - name: DATA_KAFKA_GROUP_ID
              value: "data-service"
            - name: DATA_JWT_PUBLIC_KEY_PATH
              value: "/etc/secrets/jwt-public-key.pem"
            - name: DATA_ENCRYPTION_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ include "clario360.fullname" . }}-encryption-key
                  key: ENCRYPTION_KEY
            - name: DATA_MINIO_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ include "clario360.fullname" . }}-minio-credentials
                  key: ACCESS_KEY
            - name: DATA_MINIO_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ include "clario360.fullname" . }}-minio-credentials
                  key: SECRET_KEY
            - name: OTEL_SERVICE_NAME
              value: "data-service"
            {{- with .Values.dataService.extraEnv }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          {{- include "clario360.healthProbes" (dict "portName" "http" "healthPath" "/healthz" "readyPath" "/readyz") | nindent 10 }}
          resources:
            {{- toYaml .Values.dataService.resources | nindent 12 }}
          volumeMounts:
            - name: tmp
              mountPath: /tmp
            - name: jwt-public-key
              mountPath: /etc/secrets
              readOnly: true
      volumes:
        - name: tmp
          emptyDir: {}
        - name: jwt-public-key
          secret:
            secretName: {{ include "clario360.fullname" . }}-jwt-keys
            items:
              - key: AUTH_RSA_PUBLIC_KEY_PEM
                path: jwt-public-key.pem
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              {{- include "clario360.componentSelectorLabels" (dict "root" . "component" "data-service") | nindent 14 }}
      {{- with .Values.dataService.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.dataService.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.dataService.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
