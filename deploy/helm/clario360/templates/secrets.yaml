{{/*
Secrets — uses the existingSecret pattern.
If the operator provides an existingSecret name the chart will NOT create the
Secret resource; instead Deployments must reference that pre-existing Secret.
When no existingSecret is set the chart creates the Secret with base64
placeholder values that MUST be overridden at install time via --set or a
sealed-secrets / external-secrets workflow.
*/}}

{{/* ================================================================
     Database Credentials
     ================================================================ */}}
{{- if not .Values.secrets.database.existingSecret }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "clario360.fullname" . }}-database-credentials
  namespace: {{ include "clario360.namespace" . }}
  labels:
    {{- include "clario360.labels" . | nindent 4 }}
    app.kubernetes.io/component: database
  annotations:
    helm.sh/hook-weight: "-10"
type: Opaque
data:
  DATABASE_USER: {{ .Values.secrets.database.user | default "clario360" | b64enc | quote }}
  DATABASE_PASSWORD: {{ required "secrets.database.password is required when secrets.database.existingSecret is not set" .Values.secrets.database.password | b64enc | quote }}
  # Per-service database URLs (built at install time)
  IAM_DB_URL: {{ include "clario360.databaseUrl" (dict "root" . "dbName" (default "iam" .Values.secrets.database.iamDb)) | b64enc | quote }}
  PLATFORM_DB_URL: {{ include "clario360.databaseUrl" (dict "root" . "dbName" (default "platform" .Values.secrets.database.platformDb)) | b64enc | quote }}
  CYBER_DB_URL: {{ include "clario360.databaseUrl" (dict "root" . "dbName" (default "cyber" .Values.secrets.database.cyberDb)) | b64enc | quote }}
  DATA_DB_URL: {{ include "clario360.databaseUrl" (dict "root" . "dbName" (default "dataservice" .Values.secrets.database.dataDb)) | b64enc | quote }}
  ACTA_DB_URL: {{ include "clario360.databaseUrl" (dict "root" . "dbName" (default "acta" .Values.secrets.database.actaDb)) | b64enc | quote }}
  LEX_DB_URL: {{ include "clario360.databaseUrl" (dict "root" . "dbName" (default "lex" .Values.secrets.database.lexDb)) | b64enc | quote }}
  VISUS_DB_URL: {{ include "clario360.databaseUrl" (dict "root" . "dbName" (default "visus" .Values.secrets.database.visusDb)) | b64enc | quote }}
  LICENSE_DB_URL: {{ include "clario360.databaseUrl" (dict "root" . "dbName" (default "license_db" .Values.secrets.database.licenseDb)) | b64enc | quote }}
  AUTOMATION_DB_URL: {{ include "clario360.databaseUrl" (dict "root" . "dbName" (default "automation_db" .Values.secrets.database.automationDb)) | b64enc | quote }}
{{- end }}
---
{{/* ================================================================
     Redis Credentials
     ================================================================ */}}
{{- if not .Values.secrets.redis.existingSecret }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "clario360.fullname" . }}-redis-credentials
  namespace: {{ include "clario360.namespace" . }}
  labels:
    {{- include "clario360.labels" . | nindent 4 }}
    app.kubernetes.io/component: redis
type: Opaque
data:
  REDIS_PASSWORD: {{ required "secrets.redis.password is required when secrets.redis.existingSecret is not set" .Values.secrets.redis.password | b64enc | quote }}
  REDIS_URL: {{ include "clario360.redisUrl" . | b64enc | quote }}
{{- end }}
---
{{/* ================================================================
     Kafka Credentials
     ================================================================ */}}
{{- if not .Values.secrets.kafka.existingSecret }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "clario360.fullname" . }}-kafka-credentials
  namespace: {{ include "clario360.namespace" . }}
  labels:
    {{- include "clario360.labels" . | nindent 4 }}
    app.kubernetes.io/component: kafka
type: Opaque
data:
  KAFKA_SASL_USERNAME: {{ required "secrets.kafka.saslUsername is required when secrets.kafka.existingSecret is not set" .Values.secrets.kafka.saslUsername | b64enc | quote }}
  KAFKA_SASL_PASSWORD: {{ required "secrets.kafka.saslPassword is required when secrets.kafka.existingSecret is not set" .Values.secrets.kafka.saslPassword | b64enc | quote }}
{{- end }}
---
{{/* ================================================================
     JWT Signing Keys
     ================================================================ */}}
{{- if not .Values.secrets.jwt.existingSecret }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "clario360.fullname" . }}-jwt-keys
  namespace: {{ include "clario360.namespace" . }}
  labels:
    {{- include "clario360.labels" . | nindent 4 }}
    app.kubernetes.io/component: iam
type: Opaque
data:
  AUTH_RSA_PRIVATE_KEY_PEM: {{ required "secrets.jwt.rsaPrivateKeyPem is required when secrets.jwt.existingSecret is not set" .Values.secrets.jwt.rsaPrivateKeyPem | b64enc | quote }}
  AUTH_RSA_PUBLIC_KEY_PEM: {{ required "secrets.jwt.rsaPublicKeyPem is required when secrets.jwt.existingSecret is not set" .Values.secrets.jwt.rsaPublicKeyPem | b64enc | quote }}
{{- end }}
---
{{/* ================================================================
     Encryption Key (AES-256)
     ================================================================ */}}
{{- if not .Values.secrets.encryption.existingSecret }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "clario360.fullname" . }}-encryption-key
  namespace: {{ include "clario360.namespace" . }}
  labels:
    {{- include "clario360.labels" . | nindent 4 }}
    app.kubernetes.io/component: encryption
type: Opaque
data:
  ENCRYPTION_KEY: {{ required "secrets.encryption.key is required when secrets.encryption.existingSecret is not set" .Values.secrets.encryption.key | b64enc | quote }}
{{- end }}
---
{{/* ================================================================
     MinIO / Object Storage Credentials
     ================================================================ */}}
{{- if not .Values.secrets.minio.existingSecret }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "clario360.fullname" . }}-minio-credentials
  namespace: {{ include "clario360.namespace" . }}
  labels:
    {{- include "clario360.labels" . | nindent 4 }}
    app.kubernetes.io/component: minio
type: Opaque
data:
  MINIO_ACCESS_KEY: {{ required "secrets.minio.accessKey is required when secrets.minio.existingSecret is not set" .Values.secrets.minio.accessKey | b64enc | quote }}
  MINIO_SECRET_KEY: {{ required "secrets.minio.secretKey is required when secrets.minio.existingSecret is not set" .Values.secrets.minio.secretKey | b64enc | quote }}
{{- end }}
---
{{/* ================================================================
     SMTP Credentials
     ================================================================ */}}
{{- if not .Values.secrets.smtp.existingSecret }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "clario360.fullname" . }}-smtp-credentials
  namespace: {{ include "clario360.namespace" . }}
  labels:
    {{- include "clario360.labels" . | nindent 4 }}
    app.kubernetes.io/component: smtp
type: Opaque
data:
  SMTP_USERNAME: {{ required "secrets.smtp.username is required when secrets.smtp.existingSecret is not set" .Values.secrets.smtp.username | b64enc | quote }}
  SMTP_PASSWORD: {{ required "secrets.smtp.password is required when secrets.smtp.existingSecret is not set" .Values.secrets.smtp.password | b64enc | quote }}
{{- end }}
---
{{/* ================================================================
     Notification Secrets
     ================================================================ */}}
{{- if not .Values.secrets.notification.existingSecret }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "clario360.fullname" . }}-notification-secrets
  namespace: {{ include "clario360.namespace" . }}
  labels:
    {{- include "clario360.labels" . | nindent 4 }}
    app.kubernetes.io/component: notification
type: Opaque
data:
  NOTIF_WEBHOOK_HMAC_SECRET: {{ required "secrets.notification.webhookHmacSecret is required when secrets.notification.existingSecret is not set" .Values.secrets.notification.webhookHmacSecret | b64enc | quote }}
  NOTIF_SENDGRID_API_KEY: {{ .Values.secrets.notification.sendgridApiKey | default "" | b64enc | quote }}
{{- end }}
