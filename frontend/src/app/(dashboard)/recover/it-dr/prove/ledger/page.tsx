// IT DR · Prove · Ledger.
//
// Preserves the legacy `/dr/prove/ledger` deep link under the new Recover route
// (the legacy parent permanently redirects to the Recover Prove surface).
// Re-exports the existing page — no logic is forked.
export { default } from '../../../../dr/prove/ledger/page';
