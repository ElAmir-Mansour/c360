# Watheeq RTM Coverage Map

Source workbook: `Clario360 Watheeq RTM v2.xlsx`

Generated from workbook review on 2026-06-14. This artifact maps the workbook to the current repo surfaces and the Watheeq/Lex implementation hooks present in this branch.

## Executive Summary

| Area | Workbook count | Current coverage readout |
| --- | ---: | --- |
| Traceability Matrix | 12 | 9 Delivered, 2 Partial, 1 Planned in workbook; repo now has acceptance evidence for the full contract lifecycle, contract briefs, linked contract workflow task decisions, persistent Watheeq DoA approval policies, route-backed clause/regulation governance decisions, Workflow Core Forms admin authoring API, Watheeq DoA/form/out-of-office workflow payloads and UI controls, Workflow approval-chain quorum/delegation substrate, approval-policy routing analytics, matter conflict checks, matter/obligation reports, document repository summaries, deterministic document bulk import with OCR/index metadata, fail-closed obligation reminder provider dispatch with HTTP provider config, deterministic and HTTP signature provider dispatch, signature custody/HMAC webhook validation, immutable-audit ingestion proof, and Watheeq permission/tenant guards. |
| Proposed Features | 100 | All are Proposed. 20 are NOW, 48 NEXT, 32 LATER. 13 are Must. 27 are NOW or Must and should drive acceptance sequencing. |
| API docs | OpenAPI present | `docs/api/watheeq-lex-service.openapi.yaml` documents the current `/api/v1/lex` contract and `/api/v1/watheeq` alias, including contract briefs, renewal warnings, type classification, contract timelines, matter conflict checks, document repository summaries, document bulk import, clause/regulation governance decisions, approval-policy catalog/recommendation/analytics, workflow decisions, matter/obligation reports, reminder dispatch/provider config, and signature custody. |
| UI/client hooks | Route-backed | UI pages exist for `/lex`, `/lex/contracts`, `/lex/contracts/[id]`, `/lex/documents`, `/lex/compliance`, `/lex/matters`, `/lex/obligations`, `/lex/workflow-policies`, `/lex/reports`, `/lex/signatures`, `/lex/clause-library`, and `/lex/regulations`; frontend client covers current Lex service routes plus backend-backed contract brief, renewal warning, type classification, contract timeline, document repository summary and bulk import, matter conflict check, matters, obligations, clause library governance, regulation governance, playbooks/deviations, reports, signatures endpoints including recipient rendering, approval-policy endpoints and analytics, and typed Watheeq workflow approval payloads. |

## Current Route Inventory

Runtime route name is still Lex while product entitlement and workbook use Watheeq.

| Surface | Current hook |
| --- | --- |
| Gateway | `/api/v1/lex` and `/api/v1/watheeq` -> `lex-service`, entitlement `app.watheeq` |
| Backend handler | `backend/internal/lex/handler/routes.go` |
| Frontend client | `frontend/src/lib/enterprise/api.ts` `lex` namespace |
| Frontend pages | `/lex`, `/lex/contracts`, `/lex/contracts/[id]`, `/lex/documents`, `/lex/compliance`, `/lex/matters`, `/lex/obligations`, `/lex/workflow-policies`, `/lex/reports`, `/lex/signatures`, `/lex/clause-library`, `/lex/regulations` |
| SDK | Python SDK exposes the expanded Lex namespace and Watheeq alias for contracts, contract briefs, documents, document repository summaries and bulk import, workflows, reports, dashboard, matters, matter conflict checks, obligations, clause library governance decisions, regulation governance decisions, signatures, and compliance. |
| API docs | Formal phase-1 OpenAPI in `docs/api/watheeq-lex-service.openapi.yaml` plus companion inventory in `docs/api/watheeq-lex-api-inventory.md`. |

Stale or inconsistent naming:

- External product/workbook name is Watheeq; incumbent UI/service naming still uses Lex internally.
- `/api/v1/watheeq` is now a documented runtime alias to the same guarded Lex handlers, while `/api/v1/lex` remains backward compatible.

## New Acceptance Evidence

| Capability | Implemented evidence |
| --- | --- |
| Contract brief | Route `GET /api/v1/lex/contracts/{id}/brief`; contract service derives executive/risk summary, top clauses, top risks, obligation signals, renewal signals, and metadata from detail plus latest analysis; frontend `getContractBrief`; Python SDK `lex.contracts.brief`; passing service test `backend/internal/lex/service/contract_service_test.go::TestBuildContractBrief_UsesDetailAnalysisClausesAndMetadata` and integration fixture `TestContractBriefAndKeyTermAcceptance`. |
| Contract intelligence | Routes `GET /api/v1/lex/contracts/renewal-warnings`, `POST /api/v1/lex/contracts/{id}/classify`, `GET /api/v1/lex/contracts/{id}/timeline`, and `GET /api/v1/lex/contracts/{id}/redline`; contract service computes configured renewal warning windows, deterministic type classification with optional write-through metadata, reverse-chronological contract/version/workflow/metadata events, and multi-version redline output; frontend overview/detail panels and Python SDK methods exist; passing service, API client, SDK, and integration acceptance tests cover the routes, including `TestContractRedlineAcrossUploadedVersionsAcceptance`. |
| Linked contract workflow task decisions | Route `POST /api/v1/lex/workflows/{workflowInstanceID}/tasks/{taskID}/decision` in `backend/internal/lex/handler/routes.go`; decision service updates workflow task, step, instance, and linked contract status in `backend/internal/lex/service/workflow_service.go`; passing integration tests `TestContractWorkflowDecisionApproveAdvancesContract` and `TestContractWorkflowDecisionRejectsWrongTenantAndAlreadyDecided` in `backend/internal/lex/integration/contract_lifecycle_test.go`. |
| Watheeq DoA/forms/out-of-office workflow approval | `POST /api/v1/lex/contracts/{id}/review` accepts inline or persisted approval policies; `GET/POST /api/v1/lex/workflow-policies/approval`, `GET /api/v1/lex/workflow-policies/approval/recommend`, and `GET /api/v1/lex/workflow-policies/approval/analytics` provide the persistent DoA catalog, recommendation, and routing analytics; workflow tasks persist DoA requirements, required field list, delegation evidence, approval-chain routing metadata, and control IDs; `GET /api/v1/lex/workflows` returns every active approval task; contract detail review dialog exposes DoA/form/out-of-office controls; `/lex/workflow-policies` provides approval-policy admin and analytics UX; passing integration tests `TestWatheeqWorkflowApprovalPolicyFormsAuthorityAndOutOfOffice` and `TestWatheeqWorkflowPersistedApprovalPolicyRoutesAndReview`. |
| Bulk workflow decisions | Route `POST /api/v1/lex/workflows/tasks/bulk-decision`; service reuses single-task `DecideTask` validation per item and returns requested/succeeded/failed counts plus item-level errors; frontend Review Queue exposes selected-task bulk actions; Python SDK `lex.workflows.bulk_decide`; passing integration test `TestWatheeqWorkflowBulkDecisionAcceptance`. |
| Matter conflict and triage | Routes `POST /api/v1/lex/matters/conflict-check` and `POST /api/v1/lex/matters/{id}/triage`; service compares proposed matter or contract signals against tenant matters/contracts and records productized intake triage evidence; frontend `checkMatterConflict`; Python SDK `lex.matters.conflict_check`; passing integration tests `TestMatterConflictCheckAcceptance` and `TestMatterTriageWorkflowAcceptance`. |
| Contract, matter, and obligation reports | Routes `GET /api/v1/lex/reports/contracts`, `GET /api/v1/lex/reports/matters`, and `GET /api/v1/lex/reports/obligations`; JSON rollups in contract, matter, and obligation services; CSV handlers cover all three report families; frontend `/lex/reports` switches between all three reports; Python SDK JSON/CSV helpers and passing service/handler/API/SDK tests cover the routes. |
| Document repository summary | Route `GET /api/v1/lex/documents/repository-summary`; document service rolls up type/status/confidentiality/category counts, folders, saved views, taxonomy, and retention posture from document metadata; frontend `getDocumentRepositorySummary`; Python SDK `lex.documents.repository_summary`; passing service test `backend/internal/lex/service/document_service_test.go::TestBuildDocumentRepositorySummaryAggregatesMetadata`. |
| Document bulk import | Route `POST /api/v1/lex/documents/bulk-import`; document service imports legacy repository records with per-item success/failure, file/version evidence, and deterministic migration/OCR/index metadata; frontend `bulkImportDocuments`; Python SDK `lex.documents.bulk_import`; passing integration test `backend/internal/lex/integration/document_repository_rtm_routes_test.go::TestDocumentBulkImportRepositoryAcceptance`. |
| Clause/regulation governance decisions | Routes `POST /api/v1/lex/clause-library/{id}/governance` and `POST /api/v1/lex/regulations/{id}/governance`; request payload accepts `submit_review`, `approve`, or `reject` plus optional activation, notes, and evidence; frontend `decideClauseLibraryGovernance`/`decideRegulationGovernance`; Python SDK `lex.clause_library.decide_governance`/`lex.regulations.decide_governance`; OpenAPI returns the updated clause or regulation envelope. |
| Autonomous obligation extraction | Route `POST /api/v1/lex/contracts/{id}/obligations/extract`; LLM enricher receives persisted contract text, emits obligation candidates without manually supplied items, merges them into committable drafts, and keeps extraction metadata; passing service test `backend/internal/lex/service/obligation_extraction_autonomous_test.go::TestExtractionChain_EnricherProducesDraftsFromContractTextWithoutManualItems` and integration test `backend/internal/lex/integration/obligation_autonomous_extraction_test.go::TestObligationRTMAutonomousExtractionCommitsLLMSuggestions`. |
| Obligation reminder provider dispatch | Routes `POST /api/v1/lex/obligations/reminders/outbox/dispatch` and `POST /api/v1/lex/obligations/reminders/outbox/{outboxId}/dispatch`; outbox table `backend/migrations/lex_db/000007_obligation_notification_outbox.up.sql`; pluggable dispatcher in `backend/internal/lex/service/obligation_service.go`; HTTP adapter in `backend/internal/lex/service/obligation_reminder_http.go`; env/config `LEX_OBLIGATION_REMINDER_PROVIDER_MODE`, `LEX_OBLIGATION_REMINDER_PROVIDER_ENDPOINT`, `LEX_OBLIGATION_REMINDER_PROVIDER_API_KEY`, and `LEX_OBLIGATION_REMINDER_PROVIDER_TIMEOUT`; sent attempts fail closed unless provider status, delivery status, provider message ID, and metadata are present; passing dispatcher tests in `backend/internal/lex/service/matter_obligation_service_test.go` and HTTP adapter tests in `backend/internal/lex/service/obligation_reminder_http_test.go`. |
| Semantic clause and regulation search | Routes `GET /api/v1/lex/clause-library/search` and `GET /api/v1/lex/regulations/search` accept `semantic=true`; repository keyword pruning is disabled for semantic mode, local deterministic concept-vector scoring adds semantic metadata/snippets, frontend clients and Python SDK serialize semantic parameters, and passing tests cover service scoring, repository predicate behavior, frontend route construction, and SDK models. |
| Signature custody and webhook validation | Route `POST /api/v1/lex/signatures/{id}/custody`; custody migration `backend/migrations/lex_db/000008_signature_custody.up.sql`; provider-event columns `backend/migrations/lex_db/000006_signature_provider_events.up.sql`; HMAC validation and custody recording in `backend/internal/lex/service/signature_service.go`; passing tests in `backend/internal/lex/service/signature_service_test.go`. |
| Signature provider dispatch proof | `backend/internal/lex/service/signature_service.go` adds a pluggable `SignatureProviderDispatcher`; `Send` fails closed unless dispatch returns provider envelope/recipient IDs and evidence metadata, then persists provider status and dispatch proof. Passing tests cover deterministic local provider dispatch, proof persistence, and configurable HTTP provider dispatch in `backend/internal/lex/service/signature_provider_http_test.go`. |
| Workflow approval-chain substrate | Shared Workflow `approval_chain` resume now supports `all`, `any`, and `n_of_m` quorum, stores approval evidence, cancels unresolved sibling tasks on terminal resolution, creates sequential next-approver tasks, and blocks unsafe delegation of unclaimed role-assigned tasks. Passing tests in `backend/internal/workflow/service/engine_service_approval_test.go`. |
| Workflow Core Forms admin authoring | Routes `GET/POST/PUT/DELETE /api/v1/workflows/forms` are mounted in `cmd/workflow-engine`; `FormService` wraps the canonical `internal/forms` store under tenant-scoped RLS transactions; handler tests prove create/list/get/update/delete transport behavior and error mapping in `backend/internal/workflow/handler/form_handler_test.go`. |
| Watheeq overview RTL/LTR and review queue UI | `/lex` overview exposes Arabic/English and RTL/LTR controls, renders route-backed dashboard, renewal-warning, workflow, regulation, compliance, and document data, and bulk-submits selected workflow tasks through `enterpriseApi.lex.bulkDecideWorkflowTasks`; passing frontend integration test `frontend/src/__tests__/integration/lex-overview-watheeq.test.tsx`. |
| Dashboard KPI fixture | `GET /api/v1/lex/dashboard`, `GET /api/v1/lex/compliance/dashboard`, and `GET /api/v1/lex/compliance/score` have isolated KPI acceptance in `backend/internal/lex/integration/dashboard_test.go::TestDashboardKPIAcceptanceFixture`. |
| Immutable audit mapping and ingestion for Watheeq events | Lex/Watheeq domain events now carry stable CloudEvents subjects and `product=watheeq`/`suite=lex` metadata in `backend/internal/lex/service/common.go`; audit consumer maps material Watheeq workflow/signature/compliance events as warning-level audit entries with resource IDs in `backend/internal/audit/consumer/event_mapper.go`; `backend/internal/audit/service/watheeq_ingestion_test.go` proves a Watheeq Lex CloudEvent ingests through `AuditService`, computes hash-chain evidence, and updates chain state. |
| Watheeq security acceptance proof | `backend/internal/lex/integration/security_acceptance_test.go` proves `/api/v1/lex` and `/api/v1/watheeq` alias parity, read/write permission guards, no-access rejection, and tenant isolation. |
| Full contract lifecycle acceptance | `backend/internal/lex/integration/contract_lifecycle_test.go::TestContractFullLifecycle` passes across create, upload/versioning, analyze, workflow review/list, workflow completion, status progression, latest analysis, clauses, and versions. |

## Traceability Matrix Coverage

| Req ID | Tier | Priority | Workbook status | Repo hooks | Acceptance gap |
| --- | --- | --- | --- | --- | --- |
| WTQ-001 | NOW | Must | Delivered | Contract CRUD, status, upload, versions, renew, dashboard; UI contract list/detail; OpenAPI contract covers the route surface; `TestContractFullLifecycle` passes for broad lifecycle acceptance. | No remaining core lifecycle acceptance gap; richer negotiated key-term schemas remain backlog. |
| WTQ-002 | NOW | Should | Delivered | `/contracts/{id}`, `/contracts/{id}/brief`, `/contracts/{id}/analysis`, clauses, versions; UI detail stepper; frontend/Python SDK contract brief clients; OpenAPI now documents the contract-detail and contract-brief envelopes plus lifecycle status enum; `TestContractBriefAndKeyTermAcceptance` proves key-term classification and brief output. | Richer negotiated key-term schemas remain backlog. |
| WTQ-003 | NOW | Should | Delivered | Backend `/contracts/{id}/redline`, typed frontend client, contract/document upload and versions; UI has a backend-backed redline preview with extracted-text fallback; OpenAPI now covers redline; `TestContractRedlineAcrossUploadedVersionsAcceptance` proves uploaded-version redline behavior. | No remaining route-backed redline acceptance fixture gap. |
| WTQ-004 | NEXT | Must | Partial | `/contracts/{id}/review`, `/workflows`, `/workflow-policies/approval`, `/workflow-policies/approval/recommend`, `/workflow-policies/approval/analytics`, `/api/v1/workflows/forms`, `/lex/workflow-policies`, and `/workflows/{workflowInstanceID}/tasks/{taskID}/decision`; workflow decision updates workflow task/instance state and advances the linked contract only after configured DoA quorum is met; Watheeq review now captures persisted or inline DoA policy, required form fields, authority evidence, out-of-office delegation, and routing analytics with backend, UI, SDK, OpenAPI, and integration-test coverage. | No remaining route-backed approval-policy workflow analytics gap. |
| MAT-001 | NOW | Should | Delivered | Backend `/matters` CRUD/status/link/triage routes, `/matters/conflict-check`, persistence, conflict-check and productized triage acceptance, `/lex/matters` page, frontend `listMatters/getMatter/checkMatterConflict`, Python SDK conflict check, API constants, and contract detail Matter Link are present. | No remaining route-backed matter triage/conflict acceptance gap. |
| OBL-001 | NOW | Should | Delivered | Backend `/obligations` CRUD/status routes, contract/matter-scoped obligation lists, deterministic and autonomous LLM-enriched `/contracts/{id}/obligations/extract`, renewal warning summary, reminder plan/sent routes, reminder outbox enqueue/delivery/dispatch routes, deterministic and HTTP provider dispatch/config tests, `/lex/obligations` page write/reminder actions, frontend typed obligation CRUD/scoped/extraction/reminder clients, Python SDK route wrappers, expiring contracts, compliance alerts, and obligation RTM integration tests are present. | Live production calendar/email adapter acceptance, attestation, and evidence-vault acceptance not proven. |
| CLB-001 | NOW | Should | Delivered | Backend `/clause-library` CRUD/search and governance-decision routes, bilingual AR/EN fields, version/deprecation metadata, deterministic semantic scoring with search metadata, `/lex/clause-library` page, frontend `listClauseLibrary/getClauseLibraryEntry/decideClauseLibraryGovernance`, Python SDK governance wrapper, and contract clause review are present. | No remaining route-backed governance decision acceptance gap. |
| CLB-002 | NOW | Should | Delivered | Backend `/regulations` CRUD and governance-decision routes, citations/authority/effective dates, regulation-to-clause mapping API, `/lex/regulations` page, frontend `listRegulations/getRegulation/decideRegulationGovernance`, Python SDK governance wrapper, compliance rule CRUD, Regulation Library UI, and Active Regulations are present. | Automated regulatory update ingestion not proven. |
| WTQ-DASH | NOW | Should | Delivered | `/dashboard`, `/contracts/stats`, compliance dashboard/score; `/lex` overview UI; OpenAPI now covers dashboard and KPI envelopes; `TestDashboardKPIAcceptanceFixture` proves KPI rollups. | No remaining dashboard/KPI acceptance fixture gap. |
| WTQ-UI | NOW | Should | Partial | Lex UI pages exist; global i18n has Watheeq copy; contract review dialog now exposes DoA policy, required approval fields, and out-of-office delegation controls; `/lex/workflow-policies` adds approval-policy administration; `/lex` overview has route-backed Arabic/RTL toggle and selected-workflow bulk decision integration coverage in `frontend/src/__tests__/integration/lex-overview-watheeq.test.tsx`. | No remaining overview-level RTL/LTR acceptance gap; broader page-by-page Arabic localization remains backlog. |
| WTQ-INT | NEXT | Must | Planned | Gateway, auth, tenant guard, `startContractReview`, workflow list, linked workflow task decision route, persistent Watheeq approval policy catalog, policy recommendation and analytics, Workflow Core Forms admin authoring API, `/lex/workflow-policies`, Watheeq DoA/form/out-of-office payloads, and productized Workflow parallel/quorum approval plus delegation substrate are present. | No remaining route-backed approval-policy workflow analytics gap. |
| WTQ-RPT | NEXT | Should | Delivered | Backend `/reports/contracts`, `/reports/matters`, and `/reports/obligations`; JSON rollups and CSV exports; typed frontend client; dashboards, stats, expiring contracts, compliance score; OpenAPI covers report responses. | No remaining RTM gap for contract/matter/obligation JSON/CSV reports; custom report builder and scheduled exports remain proposed backlog. |

## Proposed Features: Theme Coverage

The workbook contains all 100 proposed items. The JSON sidecar lists every ID by theme. Current repo hooks by theme:

| Theme | Count | NOW | Must | Current hook class |
| --- | ---: | ---: | ---: | --- |
| AI Drafting | 11 | 1 | 0 | Mostly no first-class hook; contract analysis partially supports summaries. |
| Review & Risk | 11 | 1 | 3 | Partial via analysis, clauses, compliance flags, deterministic type classification, and local pre-signature gate blocking high/critical unresolved compliance alerts; no full review playbook yet. |
| Obligations | 10 | 2 | 2 | Route-backed obligation CRUD/status, deterministic and autonomous LLM-enriched extraction commit, configurable renewal warning summary, reminder planning/sent/outbox/dispatch markers, fail-closed pluggable reminder dispatcher with deterministic and HTTP provider config, contract/matter-scoped lists, frontend obligations page write/reminder actions, Python SDK wrappers, expiry hooks, and compliance alerts; live production reminder adapter acceptance still pending. |
| Matters | 8 | 2 | 0 | Route-backed matter CRUD/status/triage, conflict check, matter-contract linking, frontend matters page/client, Python SDK conflict check, contract detail matter projection, and productized triage workflow acceptance. |
| Clause & Reg | 8 | 1 | 0 | Route-backed clause/regulation libraries with bilingual clause fields, ranked and deterministic semantic search, governance decision acceptance, version/deprecation metadata, citations, and regulation-to-clause mapping plus frontend pages/clients. |
| Workflow | 9 | 3 | 1 | Contract review start/list plus linked single and bulk workflow task decisions are implemented; persistent Watheeq approval policies, policy recommendation, and routing analytics are route-backed; Workflow Core Forms admin authoring is route-backed; `/lex/workflow-policies` provides policy administration and analytics; Watheeq-specific DoA/forms/out-of-office payloads and review-dialog controls are present; Lex review routing now materializes parallel approval tasks and only advances contracts after configured quorum. |
| Collaboration | 7 | 2 | 0 | Redline, versions, and contract timeline provide route-backed history hooks; no collaboration/negotiation portal route. |
| Signature | 6 | 1 | 1 | Backend signature envelope lifecycle, recipient actions, pluggable deterministic and HTTP provider dispatch proof, provider-event intake with HMAC validation, signed-file custody evidence, and frontend signatures page/types/client methods exist. Live Nafath/e-sign credentials and external vault indexing remain gaps. |
| Repository | 6 | 2 | 1 | Partial via contracts/documents plus document repository-summary taxonomy, folder, saved-view, retention rollups, and deterministic document bulk import with migration/OCR/index metadata; live OCR and external indexing remain pending. |
| Analytics | 7 | 1 | 0 | Dashboards/stats plus contract/matter/obligation reports exist; no custom report builder, scheduled exports, or portfolio heatmap. |
| Integrations | 7 | 1 | 1 | Platform auth/gateway, Watheeq OpenAPI, configurable HTTP signature provider dispatch, configurable HTTP obligation reminder provider dispatch, and HMAC-validated signature provider-event intake exist; live Nafath credentials, production reminder adapter acceptance, and public event webhooks remain gaps. |
| Security & NFR | 10 | 3 | 4 | Watheeq read/write permission guards, route-alias parity, tenant isolation acceptance tests, stable audit event subjects, immutable-audit consumer mapping, and audit-service hash-chain ingestion proof pass; KSA residency, deployment encryption proof, and richer ABAC attributes remain gaps. |

## NOW Or Must Queue

| Req ID | Theme | Tier | Priority | Coverage hook |
| --- | --- | --- | --- | --- |
| WTQ-AID-06 | AI Drafting | NOW | Should | Partial analysis/summarization hook only. |
| WTQ-RSK-01 | Review & Risk | NEXT | Must | Partial contract analysis hook. |
| WTQ-RSK-02 | Review & Risk | NEXT | Must | Partial clause risk hook. |
| WTQ-RSK-07 | Review & Risk | NOW | Should | `POST /contracts/{id}/classify` recommends and optionally applies contract type; frontend, Python SDK, OpenAPI, and service/API/integration tests cover the route. |
| WTQ-RSK-10 | Review & Risk | NEXT | Must | Backend signature send now fails closed when unresolved high/critical compliance alerts exist; richer pre-signature playbook evidence still pending. |
| WTQ-OBL-01 | Obligations | NEXT | Must | Backend obligation CRUD plus deterministic and autonomous LLM-enriched `/contracts/{id}/obligations/extract` commit route, frontend client/SDK wrappers, and route acceptance exist. |
| WTQ-OBL-02 | Obligations | NOW | Should | Backend obligation due-date/status fields, contract/matter scoped lists, reminder plan/sent/outbox/dispatch routes, deterministic and HTTP reminder provider config, frontend obligations write/reminder actions, SDK wrappers, and contract detail reminders exist; live production calendar/email adapter acceptance still pending. |
| WTQ-OBL-06 | Obligations | NOW | Must | `GET /contracts/renewal-warnings` computes configurable horizon/lead warning windows from renewal and expiry dates; frontend overview, Python SDK, OpenAPI, and service/API/integration tests cover the route. |
| WTQ-MAT-01 | Matters | NOW | Should | Backend matter CRUD/triage/conflict-check routes, frontend matters page/client, Python SDK conflict check, and conflict-check plus productized triage workflow acceptance exist. |
| WTQ-MAT-06 | Matters | NOW | Could | Backend matter-contract linking route plus frontend matters page/client and contract metadata projection exist. |
| WTQ-CLB-03 | Clause & Reg | NOW | Should | Backend clause-library CRUD/governance with version/deprecation metadata, deterministic semantic search metadata, frontend clause-library page/client, Python SDK wrapper, and contract clause review/readiness exist. |
| WTQ-WFL-02 | Workflow | NEXT | Must | Persistent Watheeq approval policies materialize parallel approval tasks from configured approvers and quorum (`all`, `any`, `n_of_m`); `/api/v1/workflows/forms` provides tenant-scoped Core Forms authoring; `/lex/workflow-policies` provides policy administration and analytics; workflow decisions wait until quorum is satisfied, cancel unresolved siblings on terminal resolution, and keep DoA/forms metadata on each task. |
| WTQ-WFL-03 | Workflow | NOW | Should | Workflow delegation validation blocks unsafe delegation and requires role-assigned pending tasks to be claimed first; Watheeq review supports out-of-office rerouting with evidence metadata and UI controls; persistent OOO policy management remains pending. |
| WTQ-WFL-04 | Workflow | NOW | Could | `POST /workflows/tasks/bulk-decision` applies shared approval/change/reject decisions to multiple workflow tasks with per-item success/error reporting; backend integration, frontend client/UI, Python SDK, and OpenAPI coverage exist. |
| WTQ-WFL-07 | Workflow | NOW | Could | Obligation reminder plan/sent/outbox/dispatch routes and local dry-run dispatcher exist; external production nudge delivery and workflow reminder acceptance still missing. |
| WTQ-COL-06 | Collaboration | NOW | Should | `GET /contracts/{id}/timeline` exposes contract, status, analysis, version, workflow, and metadata timeline events; frontend detail panel, Python SDK, OpenAPI, and service/API/integration tests cover the route. |
| WTQ-COL-07 | Collaboration | NOW | Could | Local signature envelopes track approval-to-signature handoff state, provider-event intake, HMAC validation, and custody events; outbound provider handoff remains pending. |
| WTQ-SIG-01 | Signature | NEXT | Must | Backend signature envelope lifecycle, aligned frontend client route, pluggable provider dispatcher with deterministic and HTTP dispatch proof, provider-event intake with HMAC validation, and custody evidence exist; live Nafath/e-sign adapter credentials remain pending. |
| WTQ-SIG-06 | Signature | NOW | Should | Signature envelope can target contracts/documents and record signed-file custody evidence with hashes/seals; external filing/search indexing remains pending. |
| WTQ-REP-01 | Repository | NOW | Must | Contract/document repository hook plus contract brief, document repository-summary, and document bulk-import route/client/SDK coverage. |
| WTQ-REP-04 | Repository | NOW | Could | Repository-summary rollups expose folders, taxonomy, saved views, retention metadata, and deterministic bulk-import migration/OCR/index metadata; live OCR provider and external indexing remain pending. |
| WTQ-ANL-01 | Analytics | NOW | Should | Dashboard plus contract/matter/obligation reports exist; broader executive portfolio analytics remain partial. |
| WTQ-INT-04 | Integrations | NOW | Must | Platform auth hook plus Watheeq read/write/no-access/tenant-isolation acceptance, configurable HTTP signature provider dispatch, configurable HTTP obligation reminder provider dispatch, and signature provider-event intake exist; live Nafath identity and production credential proof remain pending. |
| WTQ-SEC-01 | Security & NFR | NOW | Must | Watheeq route permission guards, route alias parity, and tenant isolation acceptance tests pass; richer attribute policies beyond role/tenant remain pending. |
| WTQ-SEC-02 | Security & NFR | NOW | Must | Workflow decisions, signature events, provider events, and custody events carry evidence metadata; Lex events carry stable resource subjects and Watheeq metadata into the immutable audit consumer; audit-service ingestion acceptance proves hash-chain persistence and chain-state update. |
| WTQ-SEC-03 | Security & NFR | NEXT | Must | Data residency is a deployment/compliance proof gap. |
| WTQ-SEC-04 | Security & NFR | NOW | Must | Signature custody hashes/seals and platform crypto/storage hooks exist; deployment encryption-at-rest/in-transit proof remains pending. |

## Highest-Priority Unresolved Gaps

1. Add production external integrations: live Nafath/e-sign credentials, production calendar/email reminder provider credentials/acceptance, public event webhooks, and external signed-file vault/indexing.
2. Keep security/compliance gaps honest: KSA residency, deployment encryption evidence, and richer ABAC attributes remain unproven.

Machine-readable companion: `watheeq-rtm-coverage.json`.
