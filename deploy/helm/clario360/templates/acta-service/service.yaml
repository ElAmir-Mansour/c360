{{- if .Values.actaService.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "clario360.componentName" (dict "root" . "component" "acta-service") }}
  namespace: {{ include "clario360.namespace" . }}
  labels:
    {{- include "clario360.componentLabels" (dict "root" . "component" "acta-service") | nindent 4 }}
  {{- with ((.Values.actaService).service).annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ dig "service" "type" "ClusterIP" .Values.actaService }}
  ports:
    - name: http
      port: 8086
      targetPort: http
      protocol: TCP
    - name: admin
      port: 9086
      targetPort: admin
      protocol: TCP
  selector:
    {{- include "clario360.componentSelectorLabels" (dict "root" . "component" "acta-service") | nindent 4 }}
{{- end }}
